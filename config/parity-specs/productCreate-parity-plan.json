{
  "scenarioId": "productCreate-parity-plan",
  "operationNames": [
    "productCreate"
  ],
  "scenarioStatus": "planned",
  "assertionKinds": [
    "payload-shape",
    "user-errors-parity",
    "downstream-read-parity"
  ],
  "liveCaptureFiles": [],
  "proxyRequest": {
    "documentPath": "config/parity-requests/productCreate-parity-plan.graphql",
    "variablesPath": "config/parity-requests/productCreate-parity-plan.variables.json"
  },
  "comparisonMode": "planned",
  "notes": "Proxy request scaffold now mirrors the staged rich-product create runtime slice (`title`, `status`, `vendor`, `productType`, `tags`, `descriptionHtml`, `templateSuffix`, `seo`). Next step is to capture the same safe create scenario against a dedicated dev-store write pack and compare mutation payload plus immediate downstream product read parity."
}
