{
  "scenarioId": "price-list-mutation-validation",
  "operationNames": ["priceListCreate"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "user-errors-parity"],
  "liveCaptureFiles": [
    "fixtures/conformance/harry-test-heelo.myshopify.com/2026-04/markets/price-list-mutation-validation.json"
  ],
  "runtimeTestFiles": ["tests/integration/markets-lifecycle-flow.test.ts"],
  "proxyRequest": {
    "documentPath": "config/parity-requests/markets/price-list-create-invalid-currency-validation.graphql",
    "variablesCapturePath": "$.cases[0].variables"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "Safe no-side-effect PriceList mutation validation coverage for Admin GraphQL 2026-04. Current Shopify rejects variable-bound invalid currency plus missing parent before resolver execution with top-level INVALID_VARIABLE; success-path writes and permission/access-scope branches remain documented endpoint gaps outside this validation scenario.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "invalid-currency-error-message",
        "capturePath": "$.cases[0].response.payload.errors[0].message",
        "proxyPath": "$.errors[0].message"
      },
      {
        "name": "invalid-currency-error-extensions",
        "capturePath": "$.cases[0].response.payload.errors[0].extensions",
        "proxyPath": "$.errors[0].extensions"
      }
    ]
  }
}
