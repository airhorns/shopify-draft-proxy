{
  "scenarioId": "customer-create-inline-null-input-argument-error",
  "operationNames": ["customerCreate"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "user-errors-parity"],
  "liveCaptureFiles": [
    "fixtures/conformance/very-big-test-store.myshopify.com/2025-01/customers/customer-create-inline-null-input.json"
  ],
  "proxyRequest": {
    "documentPath": "config/parity-requests/customers/customerCreate-inline-null-input-parity.graphql",
    "variablesPath": "config/parity-requests/customers/customerCreate-inline-null-input-parity.variables.json",
    "variablesCapturePath": "$.variables"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "Hand-synthesized capture mirroring Shopify's `argumentLiteralsIncompatible` envelope when `customerCreate` is invoked with a literal null `input`. The schema-driven validator in `proxy/mutation_helpers.gleam` is the single source for this error envelope; the fixture format follows the existing `orderCreate-inline-null-order` capture verbatim.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "top-level-error-message",
        "capturePath": "$.mutation.response.errors[0].message",
        "proxyPath": "$.errors[0].message"
      },
      {
        "name": "top-level-error-extensions",
        "capturePath": "$.mutation.response.errors[0].extensions",
        "proxyPath": "$.errors[0].extensions"
      }
    ]
  }
}
