{
  "scenarioId": "customer-detail-parity-plan",
  "operationNames": ["customer"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "selected-fields", "null-empty-behavior"],
  "liveCaptureFiles": ["fixtures/conformance/very-big-test-store.myshopify.com/2025-01/customers/customer-detail.json"],
  "proxyRequest": {
    "documentPath": "config/parity-requests/customers/customer-detail-parity-plan.graphql",
    "variablesPath": "config/parity-requests/customers/customer-detail-parity-plan.variables.json"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "Strict comparison target covers the captured top-level `customer(id:)` data payload for the merchant-facing fields currently supported by the local customer overlay serializer. Shopify cost extensions remain outside this contract.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "customer-detail-data",
        "capturePath": "$.data",
        "proxyPath": "$.data"
      }
    ]
  }
}
