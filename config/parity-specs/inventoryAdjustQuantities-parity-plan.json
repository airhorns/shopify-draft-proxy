{
  "scenarioId": "inventoryAdjustQuantities-parity-plan",
  "operationNames": ["inventoryAdjustQuantities"],
  "scenarioStatus": "planned",
  "assertionKinds": ["payload-shape", "user-errors-parity", "downstream-read-parity"],
  "liveCaptureFiles": [],
  "proxyRequest": {
    "documentPath": "config/parity-requests/inventoryAdjustQuantities-parity-plan.graphql",
    "variablesPath": "config/parity-requests/inventoryAdjustQuantities-parity-plan.variables.json"
  },
  "comparisonMode": "planned",
  "notes": "Proxy request scaffold now mirrors the staged available-quantity adjustment runtime slice (`name`, `reason`, `referenceDocumentUri`, per-item deltas, and payload `inventoryAdjustmentGroup` / `userErrors`). Next step is to run the same safe inventory-adjustment scenario against a dedicated dev-store write pack and compare the mutation payload together with immediate downstream product/variant/inventoryItem/count parity."
}
