{
  "scenarioId": "collectionDelete-parity-plan",
  "operationNames": [
    "collectionDelete"
  ],
  "scenarioStatus": "planned",
  "assertionKinds": [
    "payload-shape",
    "user-errors-parity",
    "downstream-read-parity"
  ],
  "liveCaptureFiles": [],
  "proxyRequest": {
    "documentPath": "config/parity-requests/collectionDelete-parity-plan.graphql",
    "variablesPath": "config/parity-requests/collectionDelete-parity-plan.variables.json"
  },
  "comparisonMode": "planned",
  "notes": "Proxy request scaffold now mirrors the staged collection-delete runtime slice (`collectionDelete(input:)` returning `deletedCollectionId` plus `userErrors`). Next step is to capture the same safe collection-delete scenario against the dev store and compare mutation payload plus immediate downstream `collection` / `collections` removal parity."
}
