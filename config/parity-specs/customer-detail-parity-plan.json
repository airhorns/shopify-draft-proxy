{
  "scenarioId": "customer-detail-parity-plan",
  "operationNames": ["customer"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "selected-fields", "null-empty-behavior"],
  "liveCaptureFiles": ["fixtures/conformance/very-big-test-store.myshopify.com/2025-01/customer-detail.json"],
  "proxyRequest": {
    "documentPath": "config/parity-requests/customer-detail-parity-plan.graphql",
    "variablesPath": "config/parity-requests/customer-detail-parity-plan.variables.json"
  },
  "comparisonMode": "capture-only",
  "notes": "Captured-only scaffold: live customer detail capture exercises top-level `customer(id:)` reads with the merchant-facing fields currently supported by the local customer overlay serializer, but this scenario is not strict-comparison-ready until explicit `comparison.targets` are declared.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": []
  },
  "blocker": {
    "kind": "captured-only-explicit-comparison-targets-needed",
    "blockerPath": null
  }
}
