{
  "scenarioId": "productOptionsCreate-parity-plan",
  "operationNames": [
    "productOptionsCreate"
  ],
  "scenarioStatus": "planned",
  "assertionKinds": [
    "payload-shape",
    "user-errors-parity",
    "downstream-read-parity"
  ],
  "liveCaptureFiles": [],
  "proxyRequest": {
    "documentPath": "config/parity-requests/productOptionsCreate-parity-plan.graphql",
    "variablesPath": "config/parity-requests/productOptionsCreate-parity-plan.variables.json"
  },
  "comparisonMode": "planned",
  "notes": "Proxy request scaffold now mirrors the staged option-create runtime slice (`productOptionsCreate(productId:, options:)` returning `product.options` with `values` and `optionValues { id name hasVariants }`). Next step is to run the same safe option-create scenario against a dedicated dev-store write pack and compare mutation payload plus immediate downstream option-read parity."
}
