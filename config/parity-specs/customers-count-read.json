{
  "scenarioId": "customers-count-read",
  "operationNames": ["customersCount"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "selected-fields", "search-filter-semantics"],
  "liveCaptureFiles": ["fixtures/conformance/very-big-test-store.myshopify.com/2025-01/customers-count.json"],
  "proxyRequest": {
    "documentPath": "config/parity-requests/customers-count-read.graphql",
    "variablesPath": "config/parity-requests/customers-count-read.variables.json"
  },
  "comparisonMode": "capture-only",
  "notes": "Live customersCount capture now exercises total plus two filtered-looking `customersCount(query:)` slices (`email:` and `state:`), and on this host Shopify treated both query fields as invalid/no-op while still returning exact total counts plus `extensions.search[]` warnings. The local proxy intentionally mirrors that conservative captured behavior and warning metadata instead of reusing the richer `customers` search grammar for counts.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": []
  },
  "blocker": {
    "kind": "explicit-comparison-targets-needed",
    "blockerPath": null
  }
}
