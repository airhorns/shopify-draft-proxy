{
  "scenarioId": "productDelete-parity-plan",
  "operationNames": [
    "productDelete"
  ],
  "scenarioStatus": "planned",
  "assertionKinds": [
    "payload-shape",
    "user-errors-parity",
    "downstream-read-parity"
  ],
  "liveCaptureFiles": [],
  "proxyRequest": {
    "documentPath": "config/parity-requests/productDelete-parity-plan.graphql",
    "variablesPath": "config/parity-requests/productDelete-parity-plan.variables.json"
  },
  "comparisonMode": "planned",
  "notes": "Proxy request scaffold now mirrors the staged runtime delete slice (`deletedProductId` plus payload `userErrors`). Next step is to run the same safe delete scenario against a dedicated dev-store write pack and compare the mutation payload together with immediate downstream `product(id:)` null/list-removal parity."
}
