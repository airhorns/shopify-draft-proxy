{
  "scenarioId": "delivery-profile-default-update",
  "operationNames": ["deliveryProfile", "deliveryProfileUpdate"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "user-errors-parity", "downstream-read-parity", "mutation-log-parity"],
  "liveCaptureFiles": [
    "fixtures/conformance/harry-test-heelo.myshopify.com/2026-04/shipping-fulfillments/delivery-profile-default-update.json"
  ],
  "runtimeTestFiles": ["tests/graphql_routes/admin_app_shipping.rs"],
  "proxyRequest": {
    "documentPath": "config/parity-requests/shipping-fulfillments/delivery-profile-default-update.graphql",
    "variablesCapturePath": "$.mutation.variables"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "Captured Admin GraphQL 2026-04 evidence that a name-input deliveryProfileUpdate against the shop default profile succeeds without changing authoritative relationships. The public payload preserves the default display name while incrementing version and returning empty userErrors. The proxy hydrates the complete existing profile through the recorded read cassette, stages the accepted update locally, and strictly compares the whole selected profile in both the mutation payload and downstream readback.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "default-profile-narrow-update-preserves-whole-resource",
        "capturePath": "$.mutation.result.payload.data",
        "proxyPath": "$.data",
        "upstreamCapturePath": null
      },
      {
        "name": "default-profile-narrow-update-whole-resource-readback",
        "capturePath": "$.readBack.result.payload.data",
        "proxyPath": "$.data",
        "upstreamCapturePath": null,
        "preserveProxyState": true,
        "proxyRequest": {
          "documentPath": "config/parity-requests/shipping-fulfillments/delivery-profile-default-update-read.graphql",
          "variablesCapturePath": "$.readBack.variables"
        }
      },
      {
        "name": "default-profile-update-log",
        "capturePath": "$.expectedProxyLog",
        "proxyLogPath": "$",
        "selectedPaths": [
          "$.entries[0].interpreted.primaryRootField",
          "$.entries[0].stagedResourceIds",
          "$.entries[0].status"
        ]
      }
    ]
  }
}
