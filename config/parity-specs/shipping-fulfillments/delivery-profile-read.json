{
  "scenarioId": "delivery-profile-read",
  "operationNames": ["deliveryProfile", "deliveryProfiles"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "selected-fields", "pagination-shape", "nullability"],
  "liveCaptureFiles": [
    "fixtures/conformance/harry-test-heelo.myshopify.com/2026-04/shipping-fulfillments/delivery-profiles-read.json"
  ],
  "proxyRequest": {
    "documentPath": "config/parity-requests/shipping-fulfillments/delivery-profile-detail-read.graphql",
    "variablesCapturePath": "$.queries.detail.variables"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "Live Shopify delivery-profile read capture for Admin GraphQL 2026-04. Strict comparison seeds normalized delivery profile, product, variant, and location state from the captured fixture and verifies detail, catalog, reverse merchant-owned catalog, and missing singular read behavior without upstream Shopify access.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "delivery-profile-detail-data",
        "capturePath": "$.queries.detail.result.payload.data",
        "proxyPath": "$.data"
      },
      {
        "name": "delivery-profiles-catalog-data",
        "capturePath": "$.queries.catalogFirst.result.payload.data",
        "proxyPath": "$.data",
        "proxyRequest": {
          "documentPath": "config/parity-requests/shipping-fulfillments/delivery-profiles-catalog-read.graphql",
          "variablesCapturePath": "$.queries.catalogFirst.variables"
        }
      },
      {
        "name": "delivery-profiles-reverse-merchant-owned-data",
        "capturePath": "$.queries.reverseMerchantOwned.result.payload.data",
        "proxyPath": "$.data",
        "proxyRequest": {
          "documentPath": "config/parity-requests/shipping-fulfillments/delivery-profiles-catalog-read.graphql",
          "variablesCapturePath": "$.queries.reverseMerchantOwned.variables"
        }
      },
      {
        "name": "delivery-profile-missing-data",
        "capturePath": "$.queries.missing.result.payload.data",
        "proxyPath": "$.data",
        "proxyRequest": {
          "documentPath": "config/parity-requests/shipping-fulfillments/delivery-profile-detail-read.graphql",
          "variablesCapturePath": "$.queries.missing.variables"
        }
      }
    ]
  }
}
