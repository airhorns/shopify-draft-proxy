{
  "scenarioId": "fulfillment-service-callback-url-update-protocol-validation",
  "operationNames": ["fulfillmentServiceCreate", "fulfillmentServiceUpdate"],
  "scenarioStatus": "captured",
  "assertionKinds": ["user-errors-parity", "payload-shape"],
  "liveCaptureFiles": [
    "fixtures/conformance/harry-test-heelo.myshopify.com/2026-04/shipping-fulfillments/fulfillment-service-callback-url-update-protocol-validation.json"
  ],
  "proxyRequest": {
    "documentPath": "config/parity-requests/shipping-fulfillments/fulfillment-service-callback-url-update-protocol-create.graphql",
    "variablesCapturePath": "$.setupCreate.variables"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "Strict parity for fulfillmentServiceUpdate callbackUrl protocol validation. The setup create uses a valid mock.shop callback URL only to produce a real fulfillment service ID, then the update target proves Shopify rejects ftp:// callbackUrl updates with the protocol-specific callbackUrl userError.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "setup-create-payload",
        "capturePath": "$.setupCreate.response.payload.data.fulfillmentServiceCreate",
        "proxyPath": "$.data.fulfillmentServiceCreate",
        "upstreamCapturePath": null,
        "expectedDifferences": [
          {
            "path": "$.fulfillmentService.id",
            "matcher": "shopify-gid:FulfillmentService",
            "reason": "The proxy creates a stable synthetic fulfillment-service ID instead of Shopify's live ID."
          },
          {
            "path": "$.fulfillmentService.location.id",
            "matcher": "shopify-gid:Location",
            "reason": "The proxy creates a stable synthetic fulfillment-service location ID instead of Shopify's live ID."
          }
        ]
      },
      {
        "name": "protocol-update-user-errors",
        "capturePath": "$.updateProtocol.response.payload.data.fulfillmentServiceUpdate",
        "proxyPath": "$.data.fulfillmentServiceUpdate",
        "upstreamCapturePath": null,
        "proxyRequest": {
          "documentPath": "config/parity-requests/shipping-fulfillments/fulfillment-service-callback-url-validation-update-protocol.graphql",
          "variables": {
            "id": {
              "fromPrimaryProxyPath": "$.data.fulfillmentServiceCreate.fulfillmentService.id"
            },
            "callbackUrl": {
              "fromCapturePath": "$.updateProtocol.variables.callbackUrl"
            }
          }
        }
      }
    ]
  }
}
