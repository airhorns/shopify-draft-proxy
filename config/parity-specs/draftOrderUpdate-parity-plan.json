{
  "scenarioId": "draft-order-update-live-parity",
  "operationNames": ["draftOrderUpdate"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "user-errors-parity", "downstream-read-parity"],
  "liveCaptureFiles": ["fixtures/conformance/very-big-test-store.myshopify.com/2025-01/draft-order-update-parity.json"],
  "proxyRequest": {
    "documentPath": "config/parity-requests/draftOrderUpdate-parity-plan.graphql",
    "variablesPath": "config/parity-requests/draftOrderUpdate-parity-plan.variables.json",
    "variablesCapturePath": "$.variables"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "Live capture updates a dedicated draft order's email, tags, custom attributes, and shipping line, then verifies immediate downstream `draftOrder(id:)` visibility. The proxy request replays captured variables against a locally seeded setup draft without sending the supported mutation upstream.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "mutation-user-errors",
        "capturePath": "$.mutation.response.data.draftOrderUpdate.userErrors",
        "proxyPath": "$.data.draftOrderUpdate.userErrors"
      },
      {
        "name": "mutation-draft-order",
        "capturePath": "$.mutation.response.data.draftOrderUpdate.draftOrder",
        "proxyPath": "$.data.draftOrderUpdate.draftOrder"
      },
      {
        "name": "downstream-draft-order",
        "capturePath": "$.downstreamRead.response.data.draftOrder",
        "proxyRequest": {
          "documentPath": "config/parity-requests/draftOrderCreate-downstream-read.graphql",
          "variables": {
            "id": {
              "fromPrimaryProxyPath": "$.data.draftOrderUpdate.draftOrder.id"
            }
          }
        },
        "proxyPath": "$.data.draftOrder"
      }
    ]
  }
}
