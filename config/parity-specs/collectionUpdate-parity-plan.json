{
  "scenarioId": "collectionUpdate-parity-plan",
  "operationNames": [
    "collectionUpdate"
  ],
  "scenarioStatus": "planned",
  "assertionKinds": [
    "payload-shape",
    "user-errors-parity",
    "downstream-read-parity"
  ],
  "liveCaptureFiles": [],
  "proxyRequest": {
    "documentPath": "config/parity-requests/collectionUpdate-parity-plan.graphql",
    "variablesPath": "config/parity-requests/collectionUpdate-parity-plan.variables.json"
  },
  "comparisonMode": "planned",
  "notes": "Proxy request scaffold now mirrors the staged collection-update runtime slice (`collectionUpdate(input:)` returning `collection { id title handle products(first: 10) { nodes { id title handle } pageInfo { hasNextPage hasPreviousPage } } }` plus `userErrors`). Next step is to capture the same safe collection-update scenario against the dev store and compare mutation payload plus immediate downstream `collection` / nested `product.collections` parity."
}
