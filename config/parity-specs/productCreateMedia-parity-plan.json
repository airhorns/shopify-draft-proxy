{
  "scenarioId": "product-create-media-live-parity",
  "operationNames": ["productCreateMedia"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "user-errors-parity", "downstream-read-parity"],
  "liveCaptureFiles": [
    "fixtures/conformance/very-big-test-store.myshopify.com/2025-01/product-create-media-parity.json"
  ],
  "proxyRequest": {
    "documentPath": "config/parity-requests/productCreateMedia-parity-plan.graphql",
    "variablesPath": "config/parity-requests/productCreateMedia-parity-plan.variables.json"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "Live capture now covers `productCreateMedia`. Shopify returns newly created image media as `UPLOADED` with null `preview.image` / `MediaImage.image` urls in the mutation payload, while the immediate downstream read can already shift the asset into a null-url `PROCESSING` state before it later becomes `READY`.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": []
  },
  "blocker": {
    "kind": "explicit-comparison-targets-needed",
    "blockerPath": null
  }
}
