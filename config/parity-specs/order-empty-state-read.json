{
  "scenarioId": "order-empty-state-read",
  "operationNames": ["order", "orders", "ordersCount"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "selected-fields", "null-empty-behavior", "pagination-shape"],
  "liveCaptureFiles": ["fixtures/conformance/very-big-test-store.myshopify.com/2025-01/order-empty-state.json"],
  "proxyRequest": {
    "documentPath": "config/parity-requests/order-empty-state-read.graphql",
    "variablesPath": "config/parity-requests/order-empty-state-read.variables.json"
  },
  "comparisonMode": "capture-only",
  "notes": "Live direct-order read capture now includes a non-empty merchant catalog/count baseline, but the local order query model can only replay staged or explicitly hydrated order nodes and cannot yet represent Shopify's pre-existing catalog count/pageInfo window. Keep this captured read scaffold blocked until order catalog hydration/count baselines are modeled without returning captured JSON directly.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": []
  },
  "blocker": {
    "kind": "captured-live-order-catalog-baseline-not-locally-modeled",
    "blockerPath": null,
    "details": {
      "blockedFields": ["orders", "ordersCount"],
      "requiredApproval": "Model pre-existing order catalog/count hydration in the local order read harness before strict comparison."
    }
  }
}
