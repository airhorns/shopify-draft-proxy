{
  "scenarioId": "collectionRemoveProducts-parity-plan",
  "operationNames": [
    "collectionRemoveProducts"
  ],
  "scenarioStatus": "planned",
  "assertionKinds": [
    "payload-shape",
    "user-errors-parity",
    "downstream-read-parity"
  ],
  "liveCaptureFiles": [],
  "proxyRequest": {
    "documentPath": "config/parity-requests/collectionRemoveProducts-parity-plan.graphql",
    "variablesPath": "config/parity-requests/collectionRemoveProducts-parity-plan.variables.json"
  },
  "comparisonMode": "planned",
  "notes": "Proxy request scaffold now mirrors the staged collection-membership remove runtime slice (`collectionRemoveProducts(id:, productIds:)` returning `job { id done }` plus `userErrors`). Next step is to capture the same safe membership-remove scenario against the dev store and compare mutation payload plus immediate downstream `collection.products` / `product.collections` parity."
}
