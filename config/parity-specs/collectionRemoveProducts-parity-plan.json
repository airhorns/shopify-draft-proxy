{
  "scenarioId": "collection-remove-products-live-parity",
  "operationNames": ["collectionRemoveProducts"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "user-errors-parity", "downstream-read-parity"],
  "liveCaptureFiles": [
    "fixtures/conformance/very-big-test-store.myshopify.com/2025-01/collection-remove-products-parity.json"
  ],
  "proxyRequest": {
    "documentPath": "config/parity-requests/collectionRemoveProducts-parity-plan.graphql",
    "variablesPath": "config/parity-requests/collectionRemoveProducts-parity-plan.variables.json",
    "variablesCapturePath": "$.mutation.variables"
  },
  "notes": "Executed by the conformance parity runner against the local staged-mutation path with live fixture variables; allowed differences are path-scoped to nondeterministic ids, timestamps, throttle metadata, or documented declared-gap fields.",
  "comparison": {
    "mode": "strict-json",
    "allowedDifferences": [
      {
        "path": "$.collectionRemoveProducts.job.id",
        "matcher": "shopify-gid:Job",
        "reason": "Shopify and the local parity harness allocate resource identifiers independently."
      }
    ],
    "targets": [
      {
        "name": "mutation-data",
        "capturePath": "$.mutation.response.data",
        "proxyPath": "$.data"
      }
    ]
  }
}
