{
  "scenarioId": "product-detail-read",
  "operationNames": ["product"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "selected-fields", "null-empty-behavior"],
  "liveCaptureFiles": ["fixtures/conformance/very-big-test-store.myshopify.com/2025-01/product-detail.json"],
  "proxyRequest": {
    "documentPath": "config/parity-requests/product-detail-read.graphql",
    "variablesPath": "config/parity-requests/product-detail-read.variables.json"
  },
  "notes": "Executed by the conformance parity runner against a local proxy harness with the captured Shopify payload supplied as the upstream read response; no live Shopify writes are performed.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "captured read payload",
        "capturePath": "$.data",
        "proxyPath": "$.data",
        "upstreamCapturePath": "$"
      }
    ]
  }
}
