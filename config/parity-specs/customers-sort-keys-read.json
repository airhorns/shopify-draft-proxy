{
  "scenarioId": "customers-sort-keys-read",
  "operationNames": ["customers"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "selected-fields", "sort-order-semantics"],
  "liveCaptureFiles": ["fixtures/conformance/very-big-test-store.myshopify.com/2025-01/customers-sort-keys.json"],
  "proxyRequest": {
    "documentPath": "config/parity-requests/customers-sort-keys-read.graphql",
    "variablesPath": "config/parity-requests/customers-sort-keys-read.variables.json"
  },
  "comparisonMode": "capture-only",
  "notes": "Captured-only scaffold: live customer sort-key capture exercises the currently supported deterministic Shopify customer sort keys `NAME`, `ID`, and `LOCATION` against the merchant-facing customer card slice used by the overlay serializer, but this scenario is not strict-comparison-ready until explicit `comparison.targets` are declared. `RELEVANCE` remains separate because it depends on query-scored ranking rather than deterministic field ordering.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": []
  },
  "blocker": {
    "kind": "captured-only-explicit-comparison-targets-needed",
    "blockerPath": null
  }
}
