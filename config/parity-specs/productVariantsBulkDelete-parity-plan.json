{
  "scenarioId": "productVariantsBulkDelete-parity-plan",
  "operationNames": ["productVariantsBulkDelete"],
  "scenarioStatus": "planned",
  "assertionKinds": ["payload-shape", "user-errors-parity", "downstream-read-parity"],
  "liveCaptureFiles": [],
  "proxyRequest": {
    "documentPath": "config/parity-requests/productVariantsBulkDelete-parity-plan.graphql",
    "variablesPath": "config/parity-requests/productVariantsBulkDelete-parity-plan.variables.json"
  },
  "comparisonMode": "planned",
  "notes": "Proxy request scaffold now mirrors the staged variant bulk-delete runtime slice (`productVariantsBulkDelete(productId:, variantsIds:)` returning product inventory aggregates, nested `product.variants`, and `userErrors`). Next step is to run the same safe variant-delete scenario against a dedicated dev-store write pack and compare the mutation payload together with immediate downstream variant/search/count parity."
}
