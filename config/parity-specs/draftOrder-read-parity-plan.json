{
  "scenarioId": "draft-order-detail-read",
  "operationNames": ["draftOrder"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "selected-fields", "downstream-read-parity"],
  "liveCaptureFiles": ["fixtures/conformance/harry-test-heelo.myshopify.com/2025-01/draft-order-detail.json"],
  "proxyRequest": {
    "documentPath": "config/parity-requests/draftOrder-read-parity-plan.graphql",
    "variablesPath": "config/parity-requests/draftOrder-read-parity-plan.variables.json",
    "variablesCapturePath": "$.variables"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "The standalone draft-order detail read is recaptured with the remaining detail fields selected and compared as one strict object. Shopify does not expose `note` on the captured DraftOrder read surface, so note parity remains covered by local mutation payload/downstream tests rather than this live detail contract.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "draft-order-detail",
        "capturePath": "$.response.data.draftOrder",
        "proxyPath": "$.data.draftOrder",
        "upstreamCapturePath": "$.response.data"
      }
    ]
  }
}
