{
  "scenarioId": "orderClose-live-parity",
  "operationNames": ["orderClose"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "downstream-read-parity"],
  "liveCaptureFiles": ["fixtures/conformance/harry-test-heelo.myshopify.com/2026-04/orders/orderClose-parity.json"],
  "proxyRequest": {
    "documentPath": "config/parity-requests/orders/orderClose-parity.graphql",
    "variablesCapturePath": "$.variables"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "Captured 2026-04 orderClose conformance slice for local staging.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "mutation-closed",
        "capturePath": "$.mutation.response.data.orderClose.order.closed",
        "proxyPath": "$.data.orderClose.order.closed"
      },
      {
        "name": "mutation-user-errors",
        "capturePath": "$.mutation.response.data.orderClose.userErrors",
        "proxyPath": "$.data.orderClose.userErrors"
      },
      {
        "name": "downstream-closed",
        "capturePath": "$.downstreamRead.response.data.order.closed",
        "proxyPath": "$.data.order.closed",
        "proxyRequest": {
          "documentPath": "config/parity-requests/orders/order-management-downstream-read.graphql",
          "variablesCapturePath": "$.downstreamRead.variables"
        }
      }
    ]
  }
}
