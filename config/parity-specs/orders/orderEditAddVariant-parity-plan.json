{
  "scenarioId": "order-edit-add-variant-live-parity",
  "operationNames": ["orderEditBegin", "orderEditAddVariant"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "user-errors-parity"],
  "liveCaptureFiles": [
    "fixtures/conformance/very-big-test-store.myshopify.com/2026-04/orders/order-edit-existing-order-happy-path.json"
  ],
  "proxyRequest": {
    "documentPath": "config/parity-requests/orders/orderEditExistingWorkflow-begin.graphql",
    "variablesCapturePath": "$.variables"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "Executable replacement for the old single-root `orderEditAddVariant` access-scope plan. It starts from the captured begin fixture, runs the local add-variant handler with the captured variant/location inputs, and compares the stable calculated-line payload and userErrors.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [
      {
        "path": "$.orderEditSession.id",
        "matcher": "shopify-gid:OrderEditSession",
        "reason": "Shopify and the local proxy allocate different order-edit session ids while preserving the Shopify GID type."
      }
    ],
    "targets": [
      {
        "name": "add-variant-payload",
        "capturePath": "$.addVariant.response.data.orderEditAddVariant",
        "proxyRequest": {
          "documentPath": "config/parity-requests/orders/orderEditExistingWorkflow-addVariant-payload.graphql",
          "variables": {
            "id": {
              "fromPrimaryProxyPath": "$.data.orderEditBegin.calculatedOrder.id"
            },
            "variantId": "gid://shopify/ProductVariant/46789254021353",
            "quantity": 1,
            "locationId": "gid://shopify/Location/68509171945",
            "allowDuplicates": false
          }
        },
        "proxyPath": "$.data.orderEditAddVariant"
      }
    ]
  }
}
