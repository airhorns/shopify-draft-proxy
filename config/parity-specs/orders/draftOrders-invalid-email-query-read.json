{
  "scenarioId": "draft-orders-invalid-email-query-read",
  "operationNames": ["draftOrders", "draftOrdersCount"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "selected-fields", "search-filter-semantics"],
  "liveCaptureFiles": [
    "fixtures/conformance/very-big-test-store.myshopify.com/2025-01/orders/draft-orders-invalid-email-query.json"
  ],
  "proxyRequest": {
    "documentPath": "config/parity-requests/orders/draftOrders-invalid-email-query-read.graphql",
    "variablesPath": "config/parity-requests/orders/draftOrders-invalid-email-query-read.variables.json"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "The proxy can reproduce Shopify's invalid email search warning extension, but the captured response also includes live catalog edges and aggregate count from the pre-existing store. Strict full-response comparison remains blocked until draft-order catalog/count baselines are modeled.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "captured invalid-email data",
        "capturePath": "$.response.data",
        "proxyPath": "$.data",
        "upstreamCapturePath": "$.response"
      },
      {
        "name": "captured invalid-email search warnings",
        "capturePath": "$.response.extensions.search",
        "proxyPath": "$.extensions.search",
        "upstreamCapturePath": "$.response"
      }
    ]
  }
}
