{
  "scenarioId": "orderCreateManualPayment-access-denied-parity",
  "operationNames": ["orderCreateManualPayment"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "access-denied-parity"],
  "liveCaptureFiles": [
    "fixtures/conformance/harry-test-heelo.myshopify.com/2026-04/orders/orderCreateManualPayment-access-denied-parity.json"
  ],
  "proxyRequest": {
    "documentPath": "config/parity-requests/orders/orderCreateManualPayment-access-denied-parity.graphql",
    "variablesCapturePath": "$.variables"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "Captured 2026-04 cold-order access denial for an amount-bearing manual payment on a non-Plus store. The cassette contains the exact combined capability/order query used before the local resolver returns Shopify's access-denied envelope.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "cold-ineligible-access-denied-envelope",
        "capturePath": "$.mutation.response",
        "proxyPath": "$",
        "selectedPaths": ["$.data.orderCreateManualPayment", "$.errors[0]"]
      }
    ]
  }
}
