{
  "scenarioId": "draft-order-delete-live-parity",
  "operationNames": ["draftOrderDelete"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "user-errors-parity", "downstream-read-parity"],
  "liveCaptureFiles": [
    "fixtures/conformance/harry-test-heelo.myshopify.com/2025-01/orders/draft-order-delete-parity.json"
  ],
  "proxyRequest": {
    "documentPath": "config/parity-requests/orders/draftOrderDelete-parity-plan.graphql",
    "variablesPath": "config/parity-requests/orders/draftOrderDelete-parity-plan.variables.json",
    "variablesCapturePath": "$.variables"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "Live capture deletes a dedicated setup draft order and verifies downstream `draftOrder(id:)` returns null. The proxy request replays captured variables against a locally seeded setup draft without sending the supported mutation upstream.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "mutation-payload",
        "capturePath": "$.mutation.response.data.draftOrderDelete",
        "proxyPath": "$.data.draftOrderDelete"
      },
      {
        "name": "downstream-deleted-draft-order",
        "capturePath": "$.downstreamRead.response.data.draftOrder",
        "proxyRequest": {
          "documentPath": "config/parity-requests/orders/draftOrderCreate-downstream-read.graphql",
          "variables": {
            "id": {
              "fromPrimaryProxyPath": "$.data.draftOrderDelete.deletedId"
            }
          }
        },
        "proxyPath": "$.data.draftOrder"
      }
    ]
  }
}
