{
  "scenarioId": "draft-orders-catalog-read",
  "operationNames": ["draftOrders"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "selected-fields", "pagination-shape"],
  "liveCaptureFiles": [
    "fixtures/conformance/very-big-test-store.myshopify.com/2025-01/orders/draft-orders-catalog.json"
  ],
  "proxyRequest": {
    "documentPath": "config/parity-requests/orders/draftOrders-read-parity-plan.graphql",
    "variablesPath": "config/parity-requests/orders/draftOrders-read-parity-plan.variables.json"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "The captured draftOrders catalog has a live pre-existing window with opaque cursors and hasNextPage=true. The local staged draft-order window can serialize nodes but does not know the larger Shopify catalog size behind the capture, so full pagination-shape parity remains blocked.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "captured draftOrders data",
        "capturePath": "$.response.data",
        "proxyPath": "$.data",
        "upstreamCapturePath": "$.response"
      }
    ]
  }
}
