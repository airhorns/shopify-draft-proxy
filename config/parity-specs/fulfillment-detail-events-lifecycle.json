{
  "scenarioId": "fulfillment-detail-events-lifecycle",
  "operationNames": ["fulfillment", "fulfillmentEventCreate", "fulfillmentTrackingInfoUpdate", "fulfillmentCancel"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "selected-fields", "downstream-read-parity", "runtime-staging"],
  "liveCaptureFiles": [
    "fixtures/conformance/harry-test-heelo.myshopify.com/2026-04/fulfillment-detail-events-lifecycle.json"
  ],
  "proxyRequest": {
    "documentPath": "config/parity-requests/fulfillment-detail-events-read.graphql",
    "variablesCapturePath": "$.detailRead.variables"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "runtimeTestFiles": ["tests/integration/order-fulfillment-flow.test.ts"],
  "notes": "HAR-235 captured Admin GraphQL 2026-04 fulfillment detail/event read-after-write behavior. The fixture records a disposable order fulfillment, successful fulfillmentEventCreate, immediate detail reads through top-level fulfillment(id:) and nested Order.fulfillments, tracking update preservation of event history, fulfillment cancel preservation of event history, and cleanup orderCancel.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [
      {
        "path": "$.events.pageInfo.startCursor",
        "ignore": true,
        "reason": "Shopify returns opaque fulfillment event connection cursors; local snapshot serializers use deterministic synthetic cursors.",
        "regrettable": true
      },
      {
        "path": "$.events.pageInfo.endCursor",
        "ignore": true,
        "reason": "Shopify returns opaque fulfillment event connection cursors; local snapshot serializers use deterministic synthetic cursors.",
        "regrettable": true
      },
      {
        "path": "$.fulfillmentLineItems.pageInfo.startCursor",
        "ignore": true,
        "reason": "Shopify returns opaque fulfillment line-item connection cursors; local snapshot serializers intentionally use null cursors for this nested connection.",
        "regrettable": true
      },
      {
        "path": "$.fulfillmentLineItems.pageInfo.endCursor",
        "ignore": true,
        "reason": "Shopify returns opaque fulfillment line-item connection cursors; local snapshot serializers intentionally use null cursors for this nested connection.",
        "regrettable": true
      }
    ],
    "targets": [
      {
        "name": "fulfillment-detail-events",
        "capturePath": "$.detailRead.response.data.fulfillment",
        "proxyPath": "$.data.fulfillment",
        "upstreamCapturePath": null
      },
      {
        "name": "order-nested-fulfillment-detail-events",
        "capturePath": "$.detailRead.response.data.order.fulfillments[0]",
        "proxyPath": "$.data.order.fulfillments[0]",
        "upstreamCapturePath": null
      }
    ]
  }
}
