{
  "scenarioId": "order-open-live-parity",
  "operationNames": ["orderOpen"],
  "scenarioStatus": "planned",
  "assertionKinds": ["payload-shape", "user-errors-parity", "downstream-read-parity", "side-effect-safety"],
  "liveCaptureFiles": [],
  "proxyRequest": {
    "documentPath": "config/parity-requests/orderOpen-parity-plan.graphql",
    "variablesPath": "config/parity-requests/orderOpen-parity-plan.variables.json"
  },
  "comparisonMode": "planned",
  "blocker": {
    "kind": "missing-live-order-open-capture",
    "blockerPath": "pending/order-lifecycle-payment-customer-conformance-blocker.md",
    "details": {
      "requiredScopes": ["write_orders"],
      "probeRoots": ["orderOpen"],
      "blockedMutations": ["orderOpen"],
      "docsUrl": "https://shopify.dev/docs/api/admin-graphql/latest/mutations/orderOpen",
      "safeProbe": "Use the parity request against gid://shopify/Order/0 first so Shopify returns validation/userErrors without reopening a merchant order.",
      "localStagingCandidate": "Can be locally staged after fixture-backed semantics by clearing local closed/read-after-write status only."
    }
  },
  "notes": "Planned lifecycle parity for `orderOpen`. This root is not locally supported yet; it remains an explicit unsupported passthrough registry entry until live validation and happy-path close/open semantics are captured."
}
