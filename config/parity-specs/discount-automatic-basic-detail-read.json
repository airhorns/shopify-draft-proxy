{
  "scenarioId": "discount-automatic-basic-detail-read",
  "operationNames": ["discountNode", "automaticDiscountNode"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "selected-fields", "nested-object-graph", "empty-connection-shape"],
  "liveCaptureFiles": [
    "fixtures/conformance/very-big-test-store.myshopify.com/2026-04/discount-automatic-basic-detail-read.json"
  ],
  "proxyRequest": {
    "documentPath": "config/parity-requests/discount-automatic-basic-detail-read.graphql",
    "variablesCapturePath": "$.variables"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "Executable parity coverage for singular native DiscountAutomaticBasic detail roots after creating a temporary automatic discount. The parity harness seeds the captured detail node as normalized discount state before issuing local discountNode/automaticDiscountNode reads.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [
      {
        "path": "$.automaticDiscountNode.events.edges[0].cursor",
        "matcher": "any-string",
        "reason": "Shopify event connection cursors are opaque; the local proxy emits stable synthetic cursors for seeded discount event records."
      },
      {
        "path": "$.automaticDiscountNode.events.pageInfo.startCursor",
        "matcher": "any-string",
        "reason": "Shopify event connection cursors are opaque; the local proxy emits stable synthetic cursors for seeded discount event records."
      },
      {
        "path": "$.automaticDiscountNode.events.pageInfo.endCursor",
        "matcher": "any-string",
        "reason": "Shopify event connection cursors are opaque; the local proxy emits stable synthetic cursors for seeded discount event records."
      }
    ],
    "targets": [
      {
        "name": "discount-automatic-basic-detail-data",
        "capturePath": "$.response.data",
        "proxyPath": "$.data",
        "upstreamCapturePath": null
      }
    ]
  }
}
