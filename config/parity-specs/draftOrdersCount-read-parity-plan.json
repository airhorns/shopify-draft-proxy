{
  "scenarioId": "draft-orders-count-read",
  "operationNames": ["draftOrdersCount"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "selected-fields"],
  "liveCaptureFiles": ["fixtures/conformance/very-big-test-store.myshopify.com/2025-01/draft-orders-count.json"],
  "proxyRequest": {
    "documentPath": "config/parity-requests/draftOrdersCount-read-parity-plan.graphql",
    "variablesPath": "config/parity-requests/draftOrdersCount-read-parity-plan.variables.json"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "The captured draftOrdersCount baseline reflects the live store's pre-existing draft-order count. The local order store only counts staged draft orders, so strict count parity remains blocked until snapshot/count hydration exists.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "captured draftOrdersCount data",
        "capturePath": "$.response.data",
        "proxyPath": "$.data",
        "upstreamCapturePath": "$.response"
      }
    ]
  }
}
