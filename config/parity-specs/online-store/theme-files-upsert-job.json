{
  "scenarioId": "online-store/theme-files-upsert-job",
  "operationNames": ["themeCreate", "themeFilesUpsert"],
  "scenarioStatus": "captured",
  "assertionKinds": ["runtime-staging", "payload-shape", "side-effect-boundary", "local-runtime-backed"],
  "liveCaptureFiles": ["fixtures/conformance/local-runtime/2026-04/online-store/theme-files-upsert-job.json"],
  "runtimeTestFiles": ["tests/graphql_routes/marketing_inventory_online_store.rs"],
  "proxyRequest": {
    "documentPath": "config/parity-requests/online-store/theme-files-upsert-job.graphql",
    "variablesCapturePath": "$.primary.variables",
    "apiVersion": "2026-04"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "theme-files-upsert-inline-null-and-url-job",
        "capturePath": "$.expected.primary.data",
        "proxyPath": "$.data"
      }
    ]
  },
  "notes": "Executable local-runtime parity for themeFilesUpsert job payload shape. The scenario stages one inline TEXT file and one URL-body file through the public GraphQL mutation path, proving inline writes include job: null while URL-body writes include a synthetic Job GID without runtime Shopify writes. Theme-limited-plan entitlement gating is documented out of scope because the proxy has no deterministic plan-limited theme state."
}
