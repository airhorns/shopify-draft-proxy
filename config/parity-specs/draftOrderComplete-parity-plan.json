{
  "scenarioId": "draft-order-complete-live-parity",
  "operationNames": ["draftOrderComplete"],
  "scenarioStatus": "planned",
  "assertionKinds": ["payload-shape", "user-errors-parity", "downstream-read-parity"],
  "liveCaptureFiles": [],
  "proxyRequest": {
    "documentPath": "config/parity-requests/draftOrderComplete-parity-plan.graphql",
    "variablesPath": "config/parity-requests/draftOrderComplete-parity-plan.variables.json"
  },
  "comparisonMode": "planned",
  "blocker": {
    "kind": "missing-live-draft-order-complete-access",
    "blockerPath": "pending/order-creation-conformance-scope-blocker.md",
    "details": {
      "requiredScopes": ["write_draft_orders"],
      "probeRoots": ["draftOrderComplete"],
      "blockedMutations": ["draftOrderComplete"],
      "requiredPermissions": ["mark-as-paid", "set-payment-terms"],
      "failingMessage": "Access denied for draftOrderComplete field. Required access: `write_draft_orders` access scope. Also: The user must have access to mark as paid, or set payment terms.",
      "activeCredentialTokenFamily": "shpca",
      "activeCredentialHeaderMode": "raw-x-shopify-access-token",
      "activeCredentialSummary": "the active conformance credential is a Shopify user access token (`shpca_...`) sent as raw `X-Shopify-Access-Token` on this host",
      "manualStoreAuthStatus": "missing",
      "manualStoreAuthTokenPath": ".manual-store-auth-token.json",
      "manualStoreAuthCachedScopes": [],
      "manualStoreAuthAssociatedUserScopes": []
    }
  },
  "notes": "Merchant-realistic draft-to-order completion scaffold. The checked-in proxy request captures draft status/ready state, invoice URL, completed timestamp, total price, line items, and the nested `DraftOrder.order` link plus key regular-order fields while still avoiding the stale non-schema top-level `DraftOrderCompletePayload.order` selection that Shopify rejects on this host. Live capture remains blocked until the conformance install can complete draft orders."
}
