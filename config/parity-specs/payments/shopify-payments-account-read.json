{
  "scenarioId": "shopify-payments-account-read",
  "operationNames": ["shopifyPaymentsAccount"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "access-denied", "payment-account-empty-state"],
  "liveCaptureFiles": [
    "fixtures/conformance/harry-test-heelo.myshopify.com/2025-01/payments/shopify-payments-account-access-denied.json"
  ],
  "proxyRequest": {
    "documentPath": "config/parity-requests/payments/shopify-payments-account-read.graphql"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "The configured conformance credential lacks read_shopify_payments/read_shopify_payments_accounts, so Shopify returns the direct account root as null with ACCESS_DENIED. Strict data comparison keeps the snapshot no-account branch aligned while targeted integration coverage exercises fixture-backed safe scalar and empty connection behavior.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "shopify-payments-account-data",
        "capturePath": "$.data",
        "proxyPath": "$.data",
        "upstreamCapturePath": null
      }
    ]
  }
}
