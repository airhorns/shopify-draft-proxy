{
  "scenarioId": "locations-catalog-read",
  "operationNames": ["locations"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "selected-fields", "pagination-shape"],
  "liveCaptureFiles": ["fixtures/conformance/very-big-test-store.myshopify.com/2025-01/locations-catalog.json"],
  "proxyRequest": {
    "documentPath": "config/parity-requests/locations-catalog-read.graphql",
    "variablesPath": "config/parity-requests/locations-catalog-read.variables.json"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "Executable read parity for top-level `locations` pagination. The local overlay harness receives the captured Shopify response as its upstream live-read payload and must preserve the selected `id`, `name`, cursor, and pageInfo shape without staging or synthesizing location rows from the capture.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "locations-catalog-data",
        "capturePath": "$.data",
        "proxyPath": "$.data"
      }
    ]
  }
}
