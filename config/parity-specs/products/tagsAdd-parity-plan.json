{
  "scenarioId": "tags-add-live-parity",
  "operationNames": ["tagsAdd"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "user-errors-parity", "downstream-read-parity"],
  "liveCaptureFiles": ["fixtures/conformance/harry-test-heelo.myshopify.com/2025-01/products/tags-add-parity.json"],
  "proxyRequest": {
    "documentPath": "config/parity-requests/products/tagsAdd-parity-plan.graphql",
    "variablesPath": "config/parity-requests/products/tagsAdd-parity-plan.variables.json",
    "variablesCapturePath": "$.mutation.variables"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "HAR-427 refreshed live capture for `tagsAdd` on harry-test-heelo.myshopify.com, and the staged runtime mirrors Shopify's immediate tag-index lag for hydrated products: `product.tags` updates immediately while `products(query: \"tag:...\")` / `productsCount(query: \"tag:...\")` stay limited to hydrated/base tags. This scenario executes strict proxy-vs-Shopify comparison for mutation data and the immediate downstream tag-filtered read.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "mutation-data",
        "capturePath": "$.mutation.response.data",
        "proxyPath": "$.data"
      },
      {
        "name": "downstream-read-data",
        "capturePath": "$.downstreamRead.data",
        "proxyRequest": {
          "documentPath": "config/parity-requests/products/tagsAdd-downstream-read.graphql",
          "variables": {
            "id": {
              "fromPrimaryProxyPath": "$.data.tagsAdd.node.id"
            },
            "query": "tag:hermes-sale-1776550641138"
          }
        },
        "proxyPath": "$.data"
      }
    ]
  }
}
