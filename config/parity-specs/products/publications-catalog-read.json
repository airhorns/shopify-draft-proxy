{
  "scenarioId": "publications-catalog-read",
  "operationNames": ["publications"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "selected-fields", "pagination-shape"],
  "liveCaptureFiles": [
    "fixtures/conformance/very-big-test-store.myshopify.com/2025-01/products/publications-catalog.json"
  ],
  "proxyRequest": {
    "documentPath": "config/parity-requests/products/publications-catalog-read.graphql",
    "variablesPath": "config/parity-requests/products/publications-catalog-read.variables.json"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "Executable read parity for top-level `publications` pagination. The local overlay harness receives the captured Shopify response as its upstream live-read payload and must preserve the selected `id`, `name`, cursor, and pageInfo shape without staging or synthesizing publication rows from the capture.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "publications-catalog-data",
        "capturePath": "$.payload.data",
        "proxyPath": "$.data",
        "upstreamCapturePath": "$.payload"
      }
    ]
  }
}
