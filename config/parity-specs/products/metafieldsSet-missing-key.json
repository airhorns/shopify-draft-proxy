{
  "scenarioId": "metafields-set-missing-key",
  "operationNames": ["metafieldsSet"],
  "scenarioStatus": "captured",
  "assertionKinds": ["graphql-validation", "payload-shape"],
  "liveCaptureFiles": [
    "fixtures/conformance/harry-test-heelo.myshopify.com/2025-01/products/metafields-set-missing-key-parity.json"
  ],
  "proxyRequest": {
    "documentPath": "config/parity-requests/products/metafieldsSet-parity-plan.graphql",
    "variablesCapturePath": "$.mutation.variables"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "Missing key is a top-level GraphQL INVALID_VARIABLE response for MetafieldsSetInput variables.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [
      {
        "path": "$.errors[0].locations[0].line",
        "matcher": "any-number",
        "reason": "GraphQL validation error locations depend on document formatting; the error code, message, path, and invalid value remain strict."
      },
      {
        "path": "$.errors[0].locations[0].column",
        "matcher": "any-number",
        "reason": "GraphQL validation error locations depend on document formatting; the error code, message, path, and invalid value remain strict."
      }
    ],
    "targets": [
      {
        "name": "graphql-errors",
        "capturePath": "$.mutation.response",
        "proxyPath": "$"
      }
    ]
  }
}
