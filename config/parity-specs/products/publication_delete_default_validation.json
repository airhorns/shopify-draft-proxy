{
  "scenarioId": "publication-delete-default-validation",
  "operationNames": ["publicationDelete"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "user-errors-parity", "no-upstream-passthrough", "local-runtime-backed"],
  "liveCaptureFiles": [
    "fixtures/conformance/local-runtime/2026-04/products/publication-delete-default-validation.json"
  ],
  "runtimeTestFiles": ["tests/graphql_routes/products_saved_searches.rs"],
  "proxyRequest": {
    "documentPath": "config/parity-requests/products/publicationDelete-default-validation.graphql",
    "variablesCapturePath": "$.scenarios.defaultOnlineStore.variables",
    "apiVersion": "2026-04"
  },
  "comparisonMode": "captured-fixture",
  "notes": "Executable local-runtime publicationDelete validation proof for protecting the proxy's default Online Store publication from local deletion. This scenario is local-runtime-backed runtime fixture evidence only, not Shopify parity evidence; it remains checked in to guard the supported no-upstream local staging contract with Rust integration coverage.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "default-online-store-delete-rejected",
        "capturePath": "$.scenarios.defaultOnlineStore.response.data.publicationDelete",
        "proxyPath": "$.data.publicationDelete"
      }
    ]
  }
}
