{
  "scenarioId": "productSet-options-only-requires-variants",
  "operationNames": ["productSet"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "user-errors-parity"],
  "liveCaptureFiles": [
    "fixtures/conformance/harry-test-heelo.myshopify.com/2025-01/products/product-set-options-only-requires-variants.json"
  ],
  "proxyRequest": {
    "documentPath": "config/parity-requests/products/productSet-options-only-requires-variants.graphql",
    "variablesCapturePath": "$.mutation.variables"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "Captures Shopify's guardrail for productSet inputs that change productOptions without providing variants. Shopify rejects the mutation with product: null and field [\"input\", \"variants\"] instead of generating product-option combinations implicitly.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "mutation-data",
        "capturePath": "$.mutation.response.data",
        "proxyPath": "$.data"
      }
    ]
  }
}
