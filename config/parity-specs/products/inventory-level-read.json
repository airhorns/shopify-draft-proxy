{
  "scenarioId": "inventory-level-read",
  "operationNames": ["inventoryLevel"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "selected-fields", "inventory-level-shape"],
  "liveCaptureFiles": [
    "fixtures/conformance/very-big-test-store.myshopify.com/2025-01/products/product-variants-matrix.json"
  ],
  "proxyRequest": {
    "documentPath": "config/parity-requests/products/product-variants-read.graphql",
    "variablesPath": "config/parity-requests/products/product-variants-read.variables.json"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "Uses the captured product variants matrix as live evidence for an InventoryLevel object, then replays that captured level ID through the top-level `inventoryLevel(id:)` read path against the hydrated local product-backed inventory graph.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "top-level inventory level object",
        "capturePath": "$.data.product.variants.edges[0].node.inventoryItem.inventoryLevels.edges[0].node",
        "proxyPath": "$.data.inventoryLevel",
        "proxyRequest": {
          "documentPath": "config/parity-requests/products/inventory-level-read.graphql",
          "variables": {
            "inventoryLevelId": "gid://shopify/InventoryLevel/104875000041?inventory_item_id=50643009569001"
          }
        }
      }
    ]
  }
}
