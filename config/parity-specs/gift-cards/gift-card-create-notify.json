{
  "scenarioId": "gift-card-create-notify",
  "operationNames": ["giftCardCreate", "giftCardSendNotificationToCustomer"],
  "scenarioStatus": "captured",
  "assertionKinds": ["mutation-parity", "read-after-write", "local-runtime-backed"],
  "liveCaptureFiles": ["fixtures/conformance/local-runtime/2025-01/gift-cards/gift-card-create-notify.json"],
  "runtimeTestFiles": ["test/shopify_draft_proxy/proxy/gift_cards_mutation_test.gleam"],
  "proxyRequest": {
    "documentPath": "config/parity-requests/gift-cards/gift-card-create-notify.graphql",
    "apiVersion": "2025-01"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "create notify false stages disabled notification state",
        "capturePath": "$.expected.data",
        "proxyPath": "$.data"
      }
    ]
  },
  "notes": "Executable local-runtime parity for giftCardCreate notify=false. The follow-up customer notification root proves the staged notify flag is false by returning the disabled-notification validation error before customer-assignment validation. Existing live public-schema evidence documents that this API version does not expose the notify input or GiftCard.notify field for direct live capture through the conformance harness."
}
