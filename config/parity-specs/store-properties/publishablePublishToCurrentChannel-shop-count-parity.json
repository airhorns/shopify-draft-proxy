{
  "scenarioId": "publishablePublishToCurrentChannel-shop-count-parity",
  "operationNames": ["publishablePublishToCurrentChannel"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "user-errors-parity", "store-property-parity"],
  "liveCaptureFiles": [
    "fixtures/conformance/harry-test-heelo.myshopify.com/2025-01/store-properties/publishable-publish-current-shop-count-parity.json"
  ],
  "proxyRequest": {
    "documentPath": "config/parity-requests/store-properties/publishablePublishToCurrentChannel-shop-count-parity.graphql",
    "variablesCapturePath": "$.mutation.variables"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "Direct live parity for product-scoped `publishablePublishToCurrentChannel` selecting `shop.publicationCount`. The fixture seeds Shopify's publication catalog so the local current-publication placeholder cannot inflate the shop-level publication count.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "publishable publish current shop count payload",
        "capturePath": "$.mutation.response.data.publishablePublishToCurrentChannel",
        "proxyPath": "$.data.publishablePublishToCurrentChannel"
      }
    ]
  }
}
