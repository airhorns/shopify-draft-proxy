{
  "scenarioId": "publishablePublish-product-parity",
  "operationNames": ["publishablePublish"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "user-errors-parity", "downstream-read-parity"],
  "liveCaptureFiles": [
    "fixtures/conformance/very-big-test-store.myshopify.com/2025-01/products/product-publish-parity.json"
  ],
  "proxyRequest": {
    "documentPath": "config/parity-requests/store-properties/publishablePublish-product-parity.graphql",
    "variablesPath": "config/parity-requests/store-properties/publishablePublish-product-parity.variables.json"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "Product-scoped generic `publishablePublish` support is backed by the captured product publication aggregate fixture. The generic root returns `publishable`, but the compared product aggregate fields and userErrors are the same product publication slice captured from Shopify.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "publishable product aggregate payload",
        "capturePath": "$.aggregateSelection.response.payload.data.productPublish.product",
        "proxyPath": "$.data.publishablePublish.publishable"
      },
      {
        "name": "user errors",
        "capturePath": "$.aggregateSelection.response.payload.data.productPublish.userErrors",
        "proxyPath": "$.data.publishablePublish.userErrors"
      }
    ]
  }
}
