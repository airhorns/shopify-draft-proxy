{
  "scenarioId": "publishablePublish-shop-count-parity",
  "operationNames": ["publishablePublish"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "user-errors-parity", "store-property-parity"],
  "liveCaptureFiles": [
    "fixtures/conformance/harry-test-heelo.myshopify.com/2025-01/store-properties/publishable-publish-shop-count-parity.json"
  ],
  "proxyRequest": {
    "documentPath": "config/parity-requests/store-properties/publishablePublish-shop-count-parity.graphql",
    "variablesCapturePath": "$.mutation.variables"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "Direct live parity for product-scoped generic `publishablePublish` selecting `shop.publicationCount`. The local runtime hydrates the selected payload shop from the captured upstream hydrate call when the shop baseline is not already present in store state.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "publishable publish shop count payload",
        "capturePath": "$.mutation.response.data.publishablePublish",
        "proxyPath": "$.data.publishablePublish"
      }
    ]
  }
}
