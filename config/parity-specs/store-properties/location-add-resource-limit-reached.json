{
  "scenarioId": "location-add-resource-limit-reached",
  "operationNames": ["locationActivate", "locationAdd"],
  "scenarioStatus": "captured",
  "assertionKinds": [
    "user-errors-parity",
    "validation-parity",
    "runtime-staging",
    "local-runtime-backed",
    "no-upstream-passthrough"
  ],
  "liveCaptureFiles": [
    "fixtures/conformance/harry-test-heelo.myshopify.com/2026-04/store-properties/location-add-resource-limit-reached.json"
  ],
  "runtimeTestFiles": ["test/shopify_draft_proxy/proxy/store_properties_test.gleam"],
  "proxyRequest": {
    "apiVersion": "2026-04",
    "documentPath": "config/parity-requests/store-properties/location-activate-limit-and-relocation.graphql",
    "variablesCapturePath": "$.proxyVariables.seed"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "seed-location-limit-state",
        "capturePath": "$.expected.seed.data.locationActivate",
        "proxyPath": "$.data.locationActivate"
      },
      {
        "name": "location-add-resource-limit-guard",
        "capturePath": "$.atCapAdd.response.payload.data.locationAdd",
        "proxyPath": "$.data.locationAdd",
        "proxyRequest": {
          "apiVersion": "2026-04",
          "documentPath": "config/parity-requests/store-properties/location-add-resource-limit-reached.graphql",
          "variablesCapturePath": "$.atCapAdd.variables"
        }
      },
      {
        "name": "rejected-mutations-are-not-logged",
        "capturePath": "$.expected.emptyLog",
        "proxyLogPath": "$"
      }
    ]
  },
  "notes": "Live Admin GraphQL 2026-04 capture drives the disposable shop to its location limit, records the first over-cap locationAdd response, then cleans up every disposable location created during setup. The captured public Admin API shape is field [\"input\"], code INVALID, and message \"You have reached the maximum number of locations (200)\". The parity replay earns local limit state through a cassette-backed locationActivate hydration using the same Core-only reachedLocationLimit flag as the existing activate limit scenario; the compared locationAdd payload comes from the live at-cap Shopify response."
}
