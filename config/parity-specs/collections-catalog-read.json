{
  "scenarioId": "collections-catalog-read",
  "operationNames": ["collections"],
  "scenarioStatus": "captured",
  "assertionKinds": ["payload-shape", "selected-fields", "pagination-shape"],
  "liveCaptureFiles": ["fixtures/conformance/very-big-test-store.myshopify.com/2025-01/collections-catalog.json"],
  "proxyRequest": {
    "documentPath": "config/parity-requests/collections-catalog-read.graphql",
    "variablesPath": "config/parity-requests/collections-catalog-read.variables.json"
  },
  "notes": "Executed by the conformance parity runner against a local proxy harness with the captured Shopify payload supplied as the upstream read response; no live Shopify writes are performed.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "captured read payload",
        "capturePath": "$.data",
        "proxyPath": "$.data",
        "upstreamCapturePath": "$"
      }
    ]
  }
}
