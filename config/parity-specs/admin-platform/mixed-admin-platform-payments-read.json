{
  "scenarioId": "mixed-admin-platform-payments-read",
  "operationNames": ["job", "paymentTermsTemplates"],
  "scenarioStatus": "captured",
  "assertionKinds": ["mixed-root", "payload-shape", "selected-fields", "field-order"],
  "liveCaptureFiles": [
    "fixtures/conformance/harry-test-heelo.myshopify.com/2025-01/admin-platform/mixed-admin-platform-payments-read.json"
  ],
  "proxyRequest": {
    "documentPath": "config/parity-requests/admin-platform/mixed-admin-platform-payments-read.graphql",
    "variablesCapturePath": "$.variables"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "Representative Shopify-backed mixed-root read parity. The captured document selects the AdminPlatform `job` root and Payments `paymentTermsTemplates` root in one operation so the proxy must preserve both response keys instead of dispatching only the first root family.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [],
    "targets": [
      {
        "name": "mixed-admin-platform-payments-data",
        "capturePath": "$.response.payload.data",
        "proxyPath": "$.data",
        "upstreamCapturePath": null
      }
    ]
  }
}
