{
  "scenarioId": "productDuplicate-parity-plan",
  "operationNames": ["productDuplicate"],
  "scenarioStatus": "planned",
  "assertionKinds": ["payload-shape", "user-errors-parity", "downstream-read-parity"],
  "liveCaptureFiles": [],
  "proxyRequest": {
    "documentPath": "config/parity-requests/productDuplicate-parity-plan.graphql",
    "variablesPath": "config/parity-requests/productDuplicate-parity-plan.variables.json"
  },
  "notes": "Proxy request scaffold now mirrors the staged duplicate-of-effective-product-graph runtime slice (detail fields plus duplicated options, variants, collections, media, and metafields). Next step is to capture the same safe duplicate scenario against a dedicated dev-store write pack and compare both mutation payload and immediate downstream read parity."
}
