{
  "scenarioId": "metafields-set-missing-value",
  "operationNames": ["metafieldsSet"],
  "scenarioStatus": "captured",
  "assertionKinds": ["graphql-validation", "payload-shape"],
  "liveCaptureFiles": [
    "fixtures/conformance/very-big-test-store.myshopify.com/2025-01/metafields-set-missing-value-parity.json"
  ],
  "proxyRequest": {
    "documentPath": "config/parity-requests/metafieldsSet-parity-plan.graphql",
    "variablesCapturePath": "$.mutation.variables"
  },
  "comparisonMode": "captured-vs-proxy-request",
  "notes": "Missing value is a top-level GraphQL INVALID_VARIABLE response for MetafieldsSetInput variables.",
  "comparison": {
    "mode": "strict-json",
    "expectedDifferences": [
      {
        "path": "$.errors[0].locations[0].line",
        "matcher": "any-number",
        "reason": "GraphQL validation error locations depend on document formatting; the error code, message, path, and invalid value remain strict."
      },
      {
        "path": "$.errors[0].locations[0].column",
        "matcher": "any-number",
        "reason": "GraphQL validation error locations depend on document formatting; the error code, message, path, and invalid value remain strict."
      }
    ],
    "targets": [
      {
        "name": "graphql-errors",
        "capturePath": "$.mutation.response",
        "proxyPath": "$"
      }
    ]
  }
}
