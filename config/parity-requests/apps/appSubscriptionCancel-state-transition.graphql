mutation AppSubscriptionCancelStateTransition($id: ID!) {
  appSubscriptionCancel(id: $id, prorate: true) {
    appSubscription {
      id
      status
    }
    userErrors {
      field
      message
      code
    }
  }
}
