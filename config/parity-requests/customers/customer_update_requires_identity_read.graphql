query CustomerUpdateRequiresIdentityRead($id: ID!) {
  customer(id: $id) {
    id
    firstName
    lastName
    displayName
    email
    defaultEmailAddress {
      emailAddress
    }
    defaultPhoneNumber {
      phoneNumber
    }
  }
}
