mutation MetaobjectBulkDeleteEdgeEmptyIds {
  metaobjectBulkDelete(where: { ids: [] }) {
    job {
      id
      done
    }
    userErrors {
      field
      message
      code
      elementKey
      elementIndex
    }
  }
}
