mutation MarketingEngagementCreateValidationOrderMultipleChannelSelectors(
  $channelHandle: String!
  $remoteId: String!
  $engagement: MarketingEngagementInput!
) {
  marketingEngagementCreate(channelHandle: $channelHandle, remoteId: $remoteId, marketingEngagement: $engagement) {
    marketingEngagement {
      occurredOn
    }
    userErrors {
      field
      message
      code
    }
  }
}
