query MarketingActivityRead($activityId: ID!, $remoteIds: [String!]) {
  marketingActivity(id: $activityId) {
    title
    app {
      id
      title
    }
    adSpend {
      amount
      currencyCode
    }
    marketingEvent {
      scheduledToEndAt
    }
  }
  marketingActivities(first: 10, remoteIds: $remoteIds) {
    nodes {
      title
      app {
        id
        title
      }
      adSpend {
        amount
        currencyCode
      }
      marketingEvent {
        scheduledToEndAt
      }
    }
  }
}
