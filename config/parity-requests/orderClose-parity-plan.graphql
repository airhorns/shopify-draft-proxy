mutation OrderCloseParityPlan($input: OrderCloseInput!) {
  orderClose(input: $input) {
    order {
      id
      name
      closedAt
      displayFinancialStatus
      displayFulfillmentStatus
      canMarkAsPaid
    }
    userErrors {
      field
      message
    }
  }
}
