mutation DraftOrderInvoiceSendStatusTransitionCreate($input: DraftOrderInput!) {
  draftOrderCreate(input: $input) {
    draftOrder {
      id
      status
      invoiceSentAt
      email
      invoiceUrl
    }
    userErrors {
      field
      message
    }
  }
}
