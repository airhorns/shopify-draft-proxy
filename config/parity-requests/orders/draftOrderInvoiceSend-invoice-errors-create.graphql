mutation DraftOrderInvoiceSendInvoiceErrorsCreate {
  draftOrderCreate(
    input: { lineItems: [{ title: "Invoice send local runtime item", quantity: 1, originalUnitPrice: "1.00" }] }
  ) {
    draftOrder {
      id
      status
      email
      invoiceUrl
    }
    userErrors {
      field
      message
    }
  }
}
