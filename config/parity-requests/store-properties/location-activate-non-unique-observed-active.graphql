query LocationActivateNonUniqueObservedActive($first: Int!) {
  locationsAvailableForDeliveryProfilesConnection(first: $first) {
    nodes {
      id
      name
      isActive
      isFulfillmentService
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
    }
  }
}
