mutation PublishableCurrentChannelNoCurrentChannelUnpublish($id: ID!) {
  publishableUnpublishToCurrentChannel(id: $id) {
    publishable {
      ... on Product {
        id
      }
    }
    userErrors {
      field
      message
    }
  }
}
