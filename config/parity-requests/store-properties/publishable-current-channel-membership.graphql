mutation PublishableCurrentChannelMembership($id: ID!) {
  publishablePublishToCurrentChannel(id: $id) {
    publishable {
      ... on Product {
        id
        publishedOnCurrentPublication
        resourcePublications(first: 10) {
          nodes {
            publication {
              id
            }
            isPublished
            publishable {
              ... on Product {
                id
              }
            }
          }
        }
      }
    }
    userErrors {
      field
      message
    }
  }
}
