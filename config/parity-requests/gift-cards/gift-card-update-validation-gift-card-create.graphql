mutation GiftCardUpdateValidationGiftCardCreate($input: GiftCardCreateInput!) {
  giftCardCreate(input: $input) {
    giftCard {
      id
      enabled
      customer {
        id
      }
    }
    giftCardCode
    userErrors {
      field
      message
    }
  }
}
