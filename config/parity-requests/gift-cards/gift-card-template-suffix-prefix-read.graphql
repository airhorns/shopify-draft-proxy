query GiftCardTemplateSuffixPrefixRead($id: ID!, $query: String!) {
  giftCard(id: $id) {
    id
    templateSuffix
  }
  giftCards(first: 2, query: $query, sortKey: ID) {
    nodes {
      id
      templateSuffix
    }
  }
}
