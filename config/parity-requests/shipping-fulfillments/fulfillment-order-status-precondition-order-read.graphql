query FulfillmentOrderStatusPreconditionOrderRead($orderId: ID!) {
  order(id: $orderId) {
    id
    fulfillmentOrders(first: 10) {
      nodes {
        id
        status
        updatedAt
        supportedActions {
          action
        }
      }
    }
  }
}
