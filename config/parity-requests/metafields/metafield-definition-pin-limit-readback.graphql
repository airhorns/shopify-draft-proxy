query MetafieldDefinitionPinLimitReadback($namespace: String!) {
  metafieldDefinitions(
    ownerType: PRODUCT
    first: 10
    namespace: $namespace
    pinnedStatus: ANY
    sortKey: PINNED_POSITION
  ) {
    nodes {
      key
      pinnedPosition
    }
  }
}
