query DiscountBulkSearchEffectsReadCode($id: ID!) {
  code: codeDiscountNode(id: $id) {
    id
    codeDiscount {
      __typename
      ... on DiscountCodeBasic {
        title
        status
      }
    }
  }
}
