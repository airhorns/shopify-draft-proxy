mutation DiscountBulkSearchEffectsAutomaticDelete($search: String!) {
  discountAutomaticBulkDelete(search: $search) {
    job {
      done
    }
    userErrors {
      field
      message
      code
      extraInfo
    }
  }
}
