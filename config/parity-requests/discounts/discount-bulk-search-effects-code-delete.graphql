mutation DiscountBulkSearchEffectsCodeDelete($search: String!) {
  discountCodeBulkDelete(search: $search) {
    job {
      done
    }
    userErrors {
      field
      message
      code
      extraInfo
    }
  }
}
