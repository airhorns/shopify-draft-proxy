query DiscountBulkSearchEffectsReadAutomatic($id: ID!) {
  automatic: automaticDiscountNode(id: $id) {
    id
    automaticDiscount {
      __typename
      ... on DiscountAutomaticBasic {
        title
        status
      }
    }
  }
}
