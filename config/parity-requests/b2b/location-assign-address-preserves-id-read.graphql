query B2BLocationAssignAddressPreservesIdRead($locationId: ID!) {
  companyLocation(id: $locationId) {
    id
    billingAddress {
      id
      address1
    }
    shippingAddress {
      id
      address1
    }
  }
}
