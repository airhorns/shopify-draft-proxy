query B2BContactMissingEmailCompanyRead($companyId: ID!) {
  company(id: $companyId) {
    id
    name
    externalId
    contacts(first: 5) {
      nodes {
        id
        title
        locale
        isMainContact
      }
    }
    locations(first: 5) {
      nodes {
        id
        name
      }
    }
  }
}
