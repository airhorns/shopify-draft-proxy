mutation MediaFileUpdateMixedBatch($files: [FileUpdateInput!]!) {
  fileUpdate(files: $files) {
    files {
      id
      __typename
      alt
      fileStatus
    }
    userErrors {
      field
      message
      code
    }
  }
}
