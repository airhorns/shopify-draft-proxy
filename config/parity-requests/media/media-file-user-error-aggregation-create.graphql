mutation MediaFileUserErrorAggregationCreate($files: [FileCreateInput!]!) {
  fileCreate(files: $files) {
    files {
      id
      __typename
      fileStatus
    }
    userErrors {
      field
      message
      code
    }
  }
}
