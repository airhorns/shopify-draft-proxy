query MediaFileCascadeVariantMediaRead($variantId: ID!) {
  productVariant(id: $variantId) {
    id
    media(first: 5) {
      nodes {
        id
        alt
        mediaContentType
      }
    }
  }
}
