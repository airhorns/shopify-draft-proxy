mutation NeutralMutation($fileIds: [ID!]!) {
  fileAcknowledgeUpdateFailed(fileIds: $fileIds) {
    files {
      id
      __typename
      fileStatus
    }
    userErrors {
      field
      message
      code
    }
  }
}
