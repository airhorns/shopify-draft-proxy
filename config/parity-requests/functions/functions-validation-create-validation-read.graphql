query ValidationsConnection {
  validations(first: 30) {
    nodes {
      id
      enable
      metafields(first: 5) {
        nodes {
          namespace
          key
          value
        }
      }
    }
  }
}
