query ValidationsConnection {
  validations(first: 30) {
    nodes {
      id
      enabled
      metafields(first: 5) {
        nodes {
          namespace
          key
          value
        }
      }
    }
  }
}
