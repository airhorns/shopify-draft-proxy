query FunctionOutputFieldValidationNodeInvalidRead($id: ID!) {
  node(id: $id) {
    ... on Validation {
      functionId
      functionHandle
      createdAt
      updatedAt
      enable
    }
  }
}
