#graphql
  mutation FunctionTaxAppAuthorityProbe($ready: Boolean!) {
    taxAppConfigure(ready: $ready) {
      taxAppConfiguration {
        state
      }
      userErrors {
        field
        message
        code
      }
    }
  }
