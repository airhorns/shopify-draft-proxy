mutation ValidationCreateValidationStage {
  minimal: validationCreate(validation: { functionHandle: "validation-alpha" }) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  active1: validationCreate(validation: { functionHandle: "validation-alpha", title: "Active 1", enable: true }) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  active2: validationCreate(validation: { functionHandle: "validation-alpha", title: "Active 2", enable: true }) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  active3: validationCreate(validation: { functionHandle: "validation-alpha", title: "Active 3", enable: true }) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  active4: validationCreate(validation: { functionHandle: "validation-alpha", title: "Active 4", enable: true }) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  active5: validationCreate(validation: { functionHandle: "validation-alpha", title: "Active 5", enable: true }) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  active6: validationCreate(validation: { functionHandle: "validation-alpha", title: "Active 6", enable: true }) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  active7: validationCreate(validation: { functionHandle: "validation-alpha", title: "Active 7", enable: true }) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  active8: validationCreate(validation: { functionHandle: "validation-alpha", title: "Active 8", enable: true }) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  active9: validationCreate(validation: { functionHandle: "validation-alpha", title: "Active 9", enable: true }) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  active10: validationCreate(validation: { functionHandle: "validation-alpha", title: "Active 10", enable: true }) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  active11: validationCreate(validation: { functionHandle: "validation-alpha", title: "Active 11", enable: true }) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  active12: validationCreate(validation: { functionHandle: "validation-alpha", title: "Active 12", enable: true }) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  active13: validationCreate(validation: { functionHandle: "validation-alpha", title: "Active 13", enable: true }) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  active14: validationCreate(validation: { functionHandle: "validation-alpha", title: "Active 14", enable: true }) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  active15: validationCreate(validation: { functionHandle: "validation-alpha", title: "Active 15", enable: true }) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  active16: validationCreate(validation: { functionHandle: "validation-alpha", title: "Active 16", enable: true }) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  active17: validationCreate(validation: { functionHandle: "validation-alpha", title: "Active 17", enable: true }) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  active18: validationCreate(validation: { functionHandle: "validation-alpha", title: "Active 18", enable: true }) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  active19: validationCreate(validation: { functionHandle: "validation-alpha", title: "Active 19", enable: true }) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  active20: validationCreate(validation: { functionHandle: "validation-alpha", title: "Active 20", enable: true }) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  active21: validationCreate(validation: { functionHandle: "validation-alpha", title: "Active 21", enable: true }) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  active22: validationCreate(validation: { functionHandle: "validation-alpha", title: "Active 22", enable: true }) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  active23: validationCreate(validation: { functionHandle: "validation-alpha", title: "Active 23", enable: true }) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  active24: validationCreate(validation: { functionHandle: "validation-alpha", title: "Active 24", enable: true }) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  active25: validationCreate(validation: { functionHandle: "validation-alpha", title: "Active 25", enable: true }) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  maxActive: validationCreate(validation: { functionHandle: "validation-alpha", title: "Max active", enable: true }) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  apiMismatch: validationCreate(
    validation: { functionId: "gid://shopify/ShopifyFunction/cart-beta", title: "Wrong API", enable: false }
  ) {
    validation {
      id
      enable
      blockOnFailure
      title
    }
    userErrors {
      field
      message
      code
    }
  }
  withMetafields: validationCreate(
    validation: {
      functionHandle: "validation-alpha"
      title: "With metafields"
      metafields: [{ namespace: "custom", key: "mode", type: "single_line_text_field", value: "strict" }]
    }
  ) {
    validation {
      id
      enable
      blockOnFailure
      title
      metafields(first: 5) {
        nodes {
          namespace
          key
          value
        }
      }
    }
    userErrors {
      field
      message
      code
    }
  }
}
