mutation FunctionsCartTransformRegisteredWrongApiValidationSetup($validation: ValidationCreateInput!) {
  validationCreate(validation: $validation) {
    validation {
      id
      title
    }
    userErrors {
      field
      message
      code
    }
  }
}
