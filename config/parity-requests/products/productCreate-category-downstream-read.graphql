query ProductCreateCategoryDownstreamRead($id: ID!) {
  product(id: $id) {
    id
    title
    handle
    productType
    category {
      id
      fullName
      name
      isLeaf
      level
      parentId
    }
    requiresSellingPlan
  }
}
