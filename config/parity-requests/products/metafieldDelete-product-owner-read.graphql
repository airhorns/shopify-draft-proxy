query ProductMetafieldDeleteRead($id: ID!) {
  product(id: $id) {
    id
    singularDeleted: metafield(namespace: "custom", key: "singular_delete_me") {
      id
      namespace
      key
      type
      value
    }
    metafields(first: 10, namespace: "custom") {
      nodes {
        id
        namespace
        key
        type
        value
      }
    }
  }
}
