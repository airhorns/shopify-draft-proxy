mutation PublicationUpdateLocalRuntime($id: ID!, $input: PublicationUpdateInput!) {
  publicationUpdate(id: $id, input: $input) {
    publication {
      id
      name
      autoPublish
      channel {
        id
        name
      }
    }
    userErrors {
      field
      message
      code
    }
  }
}
