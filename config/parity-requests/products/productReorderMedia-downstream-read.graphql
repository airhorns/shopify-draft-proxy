query ProductReorderMediaDownstreamRead($id: ID!) {
  product(id: $id) {
    id
    media(first: 10) {
      nodes {
        id
        alt
        mediaContentType
        status
      }
    }
    images(first: 10) {
      nodes {
        id
        altText
      }
    }
  }
}
