mutation ProductCreateCategoryParity($product: ProductCreateInput!) {
  productCreate(product: $product) {
    product {
      id
      title
      handle
      productType
      category {
        id
        fullName
        name
        isLeaf
        level
        parentId
      }
      requiresSellingPlan
    }
    userErrors {
      field
      message
    }
  }
}
