mutation ProductVariantMediaValidationProductCreateMedia($productId: ID!, $media: [CreateMediaInput!]!) {
  productCreateMedia(productId: $productId, media: $media) {
    media {
      __typename
      id
      alt
      mediaContentType
      status
    }
    mediaUserErrors {
      field
      message
    }
    product {
      id
      media(first: 10) {
        nodes {
          __typename
          id
          alt
          mediaContentType
          status
        }
      }
    }
  }
}
