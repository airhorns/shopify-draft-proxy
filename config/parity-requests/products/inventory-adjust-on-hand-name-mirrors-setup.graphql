mutation InventoryAdjustOnHandNameMirrorsSetup($input: ProductSetInput!, $synchronous: Boolean!) {
  productSet(input: $input, synchronous: $synchronous) {
    product {
      id
      totalInventory
      tracksInventory
      variants(first: 1) {
        nodes {
          id
          inventoryQuantity
          inventoryItem {
            id
          }
        }
      }
    }
    userErrors {
      field
      message
      code
    }
  }
}
