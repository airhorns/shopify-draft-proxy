query MetafieldsSetMalformedExistingDownstreamRead($id: ID!, $namespace: String!, $key: String!) {
  product(id: $id) {
    id
    malformedDigest: metafield(namespace: $namespace, key: $key) {
      id
      namespace
      key
      type
      value
      compareDigest
    }
  }
}
