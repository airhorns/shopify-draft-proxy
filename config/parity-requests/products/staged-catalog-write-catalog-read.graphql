query StagedCatalogWriteCatalogRead($first: Int!) {
  productsCount {
    count
    precision
  }
  products(first: $first, sortKey: UPDATED_AT, reverse: true) {
    nodes {
      id
      title
      handle
      status
    }
  }
}
