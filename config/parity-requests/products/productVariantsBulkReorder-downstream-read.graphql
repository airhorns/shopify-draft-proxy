query ProductVariantsBulkReorderDownstreamRead($productId: ID!) {
  product(id: $productId) {
    id
    variants(first: 10) {
      nodes {
        id
        title
        selectedOptions {
          name
          value
        }
      }
    }
  }
}
