query CollectionUpdateRuleSetJobRead($id: ID!) {
  collection(id: $id) {
    id
    title
    handle
    ruleSet {
      appliedDisjunctively
      rules {
        column
        relation
        condition
      }
    }
  }
}
