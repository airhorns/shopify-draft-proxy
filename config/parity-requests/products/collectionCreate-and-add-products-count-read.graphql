query CollectionMembershipProductsCount($id: ID!) {
  collection(id: $id) {
    id
    productsCount {
      count
      precision
    }
    products(first: 5) {
      nodes {
        id
        title
        handle
      }
    }
  }
}
