query InventoryColdVariantOrderRead($id: ID!) {
  order(id: $id) {
    id
    name
    lineItems(first: 5) {
      nodes {
        id
        quantity
        variant {
          id
        }
      }
    }
  }
}
