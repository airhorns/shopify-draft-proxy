mutation ProductInvalidSearchQueryCreate($input: ProductInput!) {
  productCreate(input: $input) {
    product {
      id
      title
      tags
      status
    }
    userErrors {
      field
      message
    }
  }
}
