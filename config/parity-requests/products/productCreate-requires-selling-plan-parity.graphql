mutation ProductCreateRequiresSellingPlanParity($product: ProductCreateInput!) {
  productCreate(product: $product) {
    product {
      id
      title
      handle
      requiresSellingPlan
      category {
        id
        fullName
      }
    }
    userErrors {
      field
      message
    }
  }
}
