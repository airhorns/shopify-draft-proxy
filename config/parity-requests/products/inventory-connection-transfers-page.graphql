query InventoryConnectionTransfersPage($query: String!, $after: String) {
  inventoryTransfers(first: 1, query: $query, sortKey: CREATED_AT, after: $after) {
    nodes {
      id
      name
      status
      totalQuantity
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
