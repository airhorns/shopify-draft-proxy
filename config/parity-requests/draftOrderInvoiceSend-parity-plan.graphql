mutation DraftOrderInvoiceSendParityPlan($id: ID!, $email: EmailInput) {
  draftOrderInvoiceSend(id: $id, email: $email) {
    draftOrder {
      id
      status
      email
      invoiceUrl
    }
    userErrors {
      field
      message
    }
  }
}
