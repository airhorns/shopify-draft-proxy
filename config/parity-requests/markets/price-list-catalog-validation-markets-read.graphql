#graphql
query PriceListCatalogValidationMarketsRead($first: Int!) {
  markets(first: $first) {
    nodes {
      id
      name
      currencySettings {
        baseCurrency {
          currencyCode
        }
      }
    }
  }
}
