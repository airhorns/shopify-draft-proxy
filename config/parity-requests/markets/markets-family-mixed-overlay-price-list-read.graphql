query MarketsFamilyMixedOverlayPriceListRead($id: ID!) {
  priceList(id: $id) {
    id
    name
    currency
    parent {
      adjustment {
        type
        value
      }
    }
    catalog {
      id
      title
      status
    }
  }
}
