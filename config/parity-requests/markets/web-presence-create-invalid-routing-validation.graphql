mutation WebPresenceCreateInvalidRouting($input: WebPresenceCreateInput!) {
  webPresenceCreate(input: $input) {
    webPresence {
      id
    }
    userErrors {
      field
      message
      code
    }
  }
}
