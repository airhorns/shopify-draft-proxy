query CatalogDeleteSuccessSetupRead($catalogId: ID!) {
  catalog(id: $catalogId) {
    __typename
    id
    title
    status
    ... on MarketCatalog {
      markets(first: 5) {
        nodes {
          id
          name
        }
      }
    }
  }
}
