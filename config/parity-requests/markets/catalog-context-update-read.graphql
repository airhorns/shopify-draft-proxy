#graphql
query CatalogContextUpdateRead($id: ID!) {
  catalog(id: $id) {
    title
    status
    ... on MarketCatalog {
      markets(first: 5) {
        nodes {
          name
        }
      }
    }
  }
  catalogsCount(type: MARKET) {
    count
    precision
  }
}
