query WebhookSubscriptionMetafieldNamespacesList($first: Int!, $uri: String!) {
  webhookSubscriptions(first: $first, uri: $uri, sortKey: ID) {
    nodes {
      id
      uri
      metafieldNamespaces
    }
  }
}
