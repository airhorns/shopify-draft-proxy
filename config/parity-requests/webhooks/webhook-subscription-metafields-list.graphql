query WebhookSubscriptionMetafieldsList($first: Int!, $uri: String!) {
  webhookSubscriptions(first: $first, uri: $uri, sortKey: ID) {
    nodes {
      id
      uri
      metafields {
        namespace
        key
      }
    }
  }
}
