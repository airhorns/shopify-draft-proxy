query WebhookSubscriptionApiVersionDetailRead($id: ID!) {
  webhookSubscription(id: $id) {
    id
    topic
    format
    uri
    callbackUrl
    createdAt
    updatedAt
    apiVersion {
      handle
      displayName
      supported
    }
  }
}
