mutation OrderCreateMandatePaymentMissingMandate($id: ID!, $idempotencyKey: String!) {
  orderCreateMandatePayment(id: $id, idempotencyKey: $idempotencyKey) {
    paymentReferenceId
    userErrors {
      field
      message
    }
  }
}
