query PaymentCustomizationMutationFirstRead($id: ID!) {
  paymentCustomization(id: $id) {
    id
    title
    enabled
    functionId
    metafields(first: 5) {
      edges {
        node {
          id
          namespace
          key
          type
          value
          createdAt
          updatedAt
        }
      }
    }
  }
}
