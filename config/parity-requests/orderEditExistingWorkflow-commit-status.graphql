mutation OrderEditExistingWorkflowCommitStatus($id: ID!, $notifyCustomer: Boolean, $staffNote: String) {
  orderEditCommit(id: $id, notifyCustomer: $notifyCustomer, staffNote: $staffNote) {
    successMessages
    userErrors {
      field
      message
    }
  }
}
