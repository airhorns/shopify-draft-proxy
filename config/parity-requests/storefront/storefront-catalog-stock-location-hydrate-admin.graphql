query StorefrontCatalogStockLocationHydrate {
  locations(first: 100, includeInactive: false) {
    nodes {
      id
      name
      isActive
      fulfillsOnlineOrders
      shipsInventory
    }
  }
}
