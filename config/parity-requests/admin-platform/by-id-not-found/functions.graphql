query FunctionByIdNotFound($shopifyFunctionId: String!, $validationId: ID!) {
  missingShopifyFunction: shopifyFunction(id: $shopifyFunctionId) {
    __typename
  }
  missingValidation: validation(id: $validationId) {
    __typename
  }
}
