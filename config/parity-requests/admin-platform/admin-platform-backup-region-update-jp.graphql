mutation BackupRegionUpdateJp {
  backupRegionUpdate(region: { countryCode: JP }) {
    backupRegion {
      __typename
      id
      name
      ... on MarketRegionCountry {
        code
      }
    }
    userErrors {
      field
      message
      code
    }
  }
}
