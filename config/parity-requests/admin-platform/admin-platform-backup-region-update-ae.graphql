mutation BackupRegionUpdateNonCa {
  backupRegionUpdate(region: { countryCode: AE }) {
    backupRegion {
      __typename
      id
      name
      ... on MarketRegionCountry {
        code
      }
    }
    userErrors {
      field
      message
      code
    }
  }
}
