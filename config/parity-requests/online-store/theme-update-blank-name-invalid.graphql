mutation ThemeUpdateBlankNameInvalid($id: ID!) {
  themeUpdate(id: $id, input: { name: "   " }) {
    theme {
      id
      name
      role
    }
    userErrors {
      field
      message
      code
    }
  }
}
