query MobilePlatformApplicationUpdateReadAndroid($id: ID!) {
  mobilePlatformApplication(id: $id) {
    __typename
    ... on AndroidApplication {
      id
      applicationId
      appLinksEnabled
      sha256CertFingerprints
    }
  }
}
