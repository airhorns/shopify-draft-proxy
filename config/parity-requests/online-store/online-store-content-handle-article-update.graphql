mutation OnlineStoreContentHandleArticleUpdate($id: ID!, $article: ArticleUpdateInput!) {
  articleUpdate(id: $id, article: $article) {
    article {
      id
      title
      handle
      blog {
        id
      }
    }
    userErrors {
      field
      message
      code
    }
  }
}
