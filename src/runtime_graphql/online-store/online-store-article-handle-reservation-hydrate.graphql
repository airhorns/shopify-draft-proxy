query OnlineStoreArticleHandleReservationHydrate($query: String!, $after: String) {
  articles(first: 250, after: $after, query: $query) {
    nodes {
      __typename
      id
      handle
      blog {
        id
      }
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
